### `wall.createComment` 🔰

Fields: **`owner_id`**, **`post_id`**, **`message`**, `from_group`, `attachments`, `reply_to_comment`

Adds a comment to a post on a user wall or community wall.

To reply to comment, set `reply_to_comment` to comment's ID you want to reply.